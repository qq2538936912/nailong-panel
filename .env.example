# 复制为项目根目录下的 `.env` 后生效（`.env` 已在 .gitignore 中，不会提交）

# --- Debian 版（依赖管理可装 glibc 版 Python/Node 运行时）---
# 配置后可直接运行 `docker compose up -d --build`，无需每次 `-f docker-compose.debian.yml`
COMPOSE_FILE=docker-compose.debian.yml
PANEL_IMAGE=xiaofeilong2/panel:debian
# 本地 build 时写入二进制/面板显示的版本号，需与 server/handler/version.go 保持一致
PANEL_VERSION=1.0.2

# --- Alpine 版（默认精简镜像）---
# COMPOSE_FILE=docker-compose.yml
# PANEL_IMAGE=xiaofeilong2/panel:latest
